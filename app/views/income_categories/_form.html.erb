<div class = "panel panel-default devise-bs new-form">
  <%= form_with(model: income_category, local: true) do |form| %>
    <div class="panel-body new-form">
      <div class="form-group">
        <%= form.label :name %>
        <%= form.text_field :name, id: :income_category_name,class: "form-control" %>
      </div>

      <div class="actions">
        <%= form.submit 'create new Income category' ,  :class => 'btn btn-primary' %>
      </div>
    </div>
      <% if income_category.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(income_category.errors.count, "error") %> prohibited this income_category from being saved:</h2>

            <ul>
              <% income_category.errors.full_messages.each do |message| %>
                  <li><%= message%></li>
              <% end %>
            </ul>
          </div>
      <% end %>
  <% end %>
</div>
